/**
 * config.ts
 *
 * Central config for the PiggyBudget app.
 * Points to the backend — swap to your live Vercel URL once deployed.
 *
 * During local development: run the Vercel backend locally with `vercel dev`
 * and use http://10.0.2.2:3000 (Android emulator) or http://localhost:3000 (iOS).
 */

export const BACKEND_URL = 'https://budget-api-sigma.vercel.app'; // ← replace with your Vercel URL after deploy
